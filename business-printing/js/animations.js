// Animations — intentionally empty.
//
// All current motion (the section fade-up) lives in css/animations.css and
// already respects prefers-reduced-motion on its own. No JS-driven animation
// exists yet, so there's nothing to wire up here. Kept as a real file rather
// than removing the <script> tag so future scroll-triggered or interactive
// animation work has an obvious home.
