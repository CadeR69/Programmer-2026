terminal commands to remember:
Update Github Repo:
# Navigate to your project folder (adjust path as needed)
cd path/to/your/project

# Check what's changed
git status

# Stage all changed files
git add .

# Commit with a message describing what you did
git commit -m "Add hamburger nav, fix container structure, style header/nav"

# Push to GitHub
git push